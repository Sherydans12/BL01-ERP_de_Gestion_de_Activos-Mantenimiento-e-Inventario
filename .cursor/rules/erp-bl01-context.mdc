---
description: Contexto del ERP TPM (BL01) — stack, docs y convenciones para agentes
alwaysApply: true
---

# ERP TPM — contexto para el agente

Este repositorio es el **TPM / BL01**: EAM, flota, mantenimiento e inventario (CPP, multi-tenant). Stack: **NestJS 11 + Prisma + PostgreSQL** en `backend/`, **Angular 18** en `frontend/`.

## Antes de cambios amplios o de dominio

1. Leé **[AGENTS.md](../../AGENTS.md)** en la raíz del repo (índice y rutas clave).
2. Revisá **[docs/agentes/README.md](../../docs/agentes/README.md)** por decisiones recientes y guías de herramientas (skills, equivalencias a claude-mem en Cursor).
3. Para datos y reglas de negocio, contrastá con **[README.md](../../README.md)** y `backend/prisma/schema.prisma`.

## Reglas de producto (obligatorio)

Seguí **`.cursor/rules/tpm-arquitectura.mdc`** (persona, tenant/site, NestJS, Angular, tokens UI TPM).

## Convenciones

- Respetá **multi-tenant** (`tenantId`) y modelos de contrato/faena existentes.
- No introduzcas dependencias o patrones que contradigan el código ya establecido en el mismo módulo; alineá con servicios y DTOs vecinos.
- Documentación nueva de equipo o de “memoria de proyecto”: preferí **`docs/agentes/`** y enlazá desde ahí en lugar de dispersar notas sueltas.

## UI (Angular)

Para trabajo visual/UX, el equipo puede usar el skill **ui-ux-pro-max-skill** si está instalado en `.cursor/skills/` o en `~/.cursor/skills/`; si el usuario no lo menciona, pedí confirmación antes de asumir un rediseño grande.
