# Dependencias
node_modules/

# Entorno y Secretos (CRÍTICO)
.env
.env.local
deploy/qa.env

# Backups de Base de Datos (volcados .sql van aquí; no uses *.sql global:
# ignoraba también backend/prisma/migrations/*.sql y rompía el deploy)
backups/

# Sistema
.DS_Store
Thumbs.db

# Logs de despliegue
*.log

# Clon temporal al actualizar skills de terceros
temp-ui-ux-pro-max-skill/
temp-everything-claude-code/
temp-ecc-survey/
temp-uiux-survey/
temp-ecc-import/
.codex-manual-cache/

# Log local de hooks Cursor (memoria automática por máquina)
docs/agentes/sesiones-auto.log.md

# Salidas locales de npm run lint:ci:report
eslint-ci-full.txt
eslint-remaining.txt
backend/eslint-ci-report.json

# Informes locales de auditoría (npm audit JSON, trivy staging, gitleaks, psql, curl) — no versionar
security-audit-reports/

# Plantilla / consolidado de inventario (uso local e importaciones; no versionar)
Consolidado de Inventario Plataforma Base Logic -  explicado fila 5.xlsx
